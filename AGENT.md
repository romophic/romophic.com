# romophic.com - Project Context

## 1. プロジェクトの概要と方針
**romophic.com** は、シンプルさ、パフォーマンス、そして純正機能を重視して構築されたミニマルな技術ブログです。
- **ミニマリズム:** 重い外部UIライブラリ（shadcn/ui や Reactコンポーネントなど）や複雑な抽象化を避けます。純正のHTML/Tailwindクラスと、階層の浅いフラットなディレクトリ構造を使用します。
- **Test as Documentation（仕様書としてのテスト）:** コードの動作を説明するために複雑なテキストドキュメントに頼ることはしません。代わりに **Vitest** を「生きた仕様書」として活用します。テストコード自体が、サイトの振る舞いを証明する究極のドキュメントとして機能します。

## 2. 技術基盤 (Tech Stack)
- **フレームワーク:** Astro v6 (完全なSSG、最小限のクライアントサイドJS)
- **スタイリング:** Tailwind CSS v4 (`@tailwindcss/vite` 経由で設定)
- **テスト (仕様書):** Vitest + Astro Container API
- **コンテンツ:** MDX (Astro純正のShikiによるコードハイライト)

## 3. ディレクトリ構造
すべてのUIコンポーネントは、見つけやすさを最大化するためにフラットに配置されています。
- `src/components/`: すべてのUIコンポーネント（`Header.astro`, `Footer.astro`, `BlogCard.astro` など）を含むフラットな構造
- `src/content/`: データソース（MDXブログ記事、著者、プロジェクト情報など）
- `src/lib/`: コアロジックとデータ取得ユーティリティ
- `src/pages/`: Astroのルーティング（ページ構成）

## 4. 開発フロー (The "Vibe Loop")
1. **計画 & 仕様定義:** Vitestで、動作を自然言語で記述したテスト（`.spec.ts`）を書く。
2. **実装:** Astroの純正機能を使ってネイティブに実装する (`pnpm dev`)。
3. **検証:** テストを実行し (`pnpm test`)、仕様を満たしていることを確認する。
4. **デプロイ:** `pnpm build` を実行する。

*シンプルに保ち、純正機能を使い、コードの正しさはテストに語らせること。*
